name: Build

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
      with:
        repository: cactu5/decentralized-ISO20022-1

    - name: Checkout to active branch
      run: |
        git fetch origin
        git checkout ${{ github.head_ref }}

    - name: Build with Maven in Docker
      run: |
        docker network create mynetwork
        docker build -f mock-bank-system/Dockerfile -t mock-bank-system
        docker ps -a
        docker run -d -p "4321:4321" --network=mynetwork mock-bank-system
        docker build -f decentralizediso20022/Dockerfile --network=mynetwork
